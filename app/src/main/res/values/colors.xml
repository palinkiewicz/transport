<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      The window background the platform paints before Compose draws its first frame. Matches the
      default Transport palette's `background`; without a night variant a dark-mode cold start
      flashes white for that frame.
    -->
    <color name="window_background">#FDF7FF</color>
</resources>
